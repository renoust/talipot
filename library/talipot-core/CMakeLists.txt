IF(WIN32)
  SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DDLL_Talipot")
ENDIF(WIN32)
  SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DTALIPOT_INSTALL_LIBDIR=\"${CMAKE_INSTALL_LIBDIR}\"")

ADD_SUBDIRECTORY(include)
ADD_SUBDIRECTORY(src)

